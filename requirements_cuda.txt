# Dépendances izvox avec support CUDA (NVIDIA)
# Installation:
#   pip install -r requirements_cuda.txt --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt

torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
