# Dépendances communes izvox
# Note: PyTorch s'installe séparément avec l'option CUDA, voir scripts/install_windows.bat

# Core
numpy>=1.24.0,<2.0.0
scipy>=1.11.0          # rééchantillonnage haute qualité (fallback numpy si absent)
pyyaml>=6.0
tqdm>=4.65.0

# Audio (Windows / WASAPI)
# pyaudiowpatch 0.2.12.8 est la derniere publiee sur PyPI au moment de
# l'ecriture ; on borne large pour absorber les futures versions sans
# refaire un install propre.
pyaudiowpatch>=0.2.12.7,<0.3
soundfile>=0.12.0

# Deep Learning (torch s'installe séparément)
transformers>=4.35.0
accelerate>=0.25.0
sentencepiece>=0.1.99

# STT
faster-whisper>=1.0.0
ctranslate2>=3.24.0

# TTS
piper-tts>=1.2.0
onnxruntime>=1.16.0

# Monitoring
psutil>=5.9.0
py-cpuinfo>=9.0.0
colorlog>=6.8.0
